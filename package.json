{
  "name": "atoo",
  "private": true,
  "version": "1.0.0",
  "description": "A→Ω — an interactive century-by-century timeline of Ancient Greece",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
